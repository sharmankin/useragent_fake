[project]
name = "useragent_fake"
description = "Fake User-Agent"
version = "0.2.0"

dependencies = [
    "requests",
    "PySocks",
    "bs4",
    "tqdm"
]

[build-system]
requires = ["setuptools>=40.8.0", "wheel"]
