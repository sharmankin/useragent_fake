[project]
name = "useragent_fake"
description = "Fake User-Agent"
version = "0.5.1"

dependencies = [
    "requests",
    "PySocks",
    "bs4",
    "tqdm"
]

[build-system]
requires = ["setuptools>=50.6.0", "wheel"]
